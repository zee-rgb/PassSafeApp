<div class="max-w-xl mx-auto mt-12">
  <h1 class="text-2xl font-semibold text-gray-900">Confirm account deletion</h1>
  <p class="mt-3 text-sm text-gray-700">
    This action will permanently delete your account and all associated data. This cannot be undone.
  </p>

  <div class="mt-6 flex items-center gap-3">
    <%= button_to "Yes, delete my account", registration_path(resource_name), method: :delete, form: { data: { turbo: false } }, class: "inline-flex items-center justify-center rounded-md bg-red-600 px-4 py-2 text-sm font-semibold text-white shadow-sm hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-red-500 focus:ring-offset-0 transition-colors" %>

    <%= link_to "Cancel", edit_user_registration_path, class: "text-sm text-gray-700 hover:text-gray-900" %>
  </div>
</div>
